--So we are back in the mine~

class Creeper {
explode(player : Player, power : Int) : Int { player.setlifestate("dead") };
};

class Bad {
a : Creeper;
me : Player;
enrage(b : Creeper) : Int {a.explode(me, b.explode(me.mine, yourbloodpressure))};
};
